# .gitlab-ci.yml
image: node:latest

cache:
  paths:
    - node_modules/

before_script:
  - cd Frontend
  - npm install

pages:
  script:
    - npm run build
    - mv public ../public
  artifacts:
    paths:
      - public
  only:
    - main